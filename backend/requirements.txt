fastapi>=0.133.0,<0.137.0
uvicorn[standard]>=0.30.6
httpx>=0.28.1
pydantic>=2.12.0
pydantic-settings>=2.6.1
sqlalchemy[asyncio]>=2.0.35
asyncpg>=0.29.0
pgvector>=0.3.5
mcp>=1.2.0,<2.0.0  # v2 renamed FastMCP -> MCPServer; app/mcp_server/server.py still uses the v1 API
python-multipart>=0.0.18
PyYAML>=6.0.2
python-dotenv>=1.0.1
ddgs>=9.14.4
docx2txt

# Local chat/generation LLM (in-process async engine, continuous batching
# for concurrent multi-user inference). Pulls its own compatible torch/CUDA
# wheels - don't pin torch separately alongside this.
vllm==0.27.1

# Hardware status (/ws/system)
psutil>=6.0
nvidia-ml-py>=12.0  # GPU stats only - safe to be absent/fail on non-NVIDIA hosts

# Voice input (local speech-to-text)
faster-whisper>=1.0

# File upload -> chunk -> vector ingestion
# (loose-pinned rather than exact - core and readers-file need to resolve to
# a mutually compatible pair, and pip is better placed to pick that than a
# hand-picked pair of exact versions)
llama-index-core>=0.12,<0.13
llama-index-readers-file>=0.4,<0.5
pypdf>=5.0
python-docx>=1.1
